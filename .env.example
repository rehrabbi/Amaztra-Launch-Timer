# AMAZTRA waitlist endpoint (public form URL, not a secret).
# Create a free form at https://formspree.io and paste its endpoint here,
# then copy this file to .env. Without it the form validates but reports
# that signups are not yet open (it never fakes a successful signup).
VITE_WAITLIST_ENDPOINT=
